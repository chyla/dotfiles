[alias]
        flog = log --show-signature --pretty=fuller --date=format:'%d-%m-%Y %H:%M:%S %z (%A, %d %b)'
        a = add
        s = status
        st = status
        c = commit
        co = checkout
        wt = worktree
        br = branch
        dif = diff
